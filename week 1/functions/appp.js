//welcome to the class Enyi

// function sayMyName(firstname){
//     console.log(`welocme to the class ${firstname}`)
// }

// let firstname='enyi'

// sayMyName(firstname)
//keyword arguments are variables passed into function at the function calling phase
//parameter are variable passed in during function declaration

// function displayDetails(firstname, age){
//     console.log(`welcome to the class ${firstname}, you are ${age} years old`)
// }

// let personName = 'jude'
// let personAge= 13

// displayDetails(personAge, personName)

//create a calc function
function calculate(num1, num2, operator) {
    if (operator === '+') 
    console.log(num1 + num2)
    if (operator === '-') 
    console.log(num1 - num2) 
    if (operator === '*')
    console.log(num1 * num2)
    if (operator === '/') 
    console.log( num1 / num2)
    if (operator === '**') 
    console.log(num1 ** num2)
    if (operator === '%') 
    console.log(num1 % num2)
}
    let num1 = 20
    let num2 = 30
    calculate(num1, num2, '+')
    calculate(num1, num2, '-')
    calculate(num1, num2, '*')
    calculate(num1, num2, '/')
    calculate(num1, num2, '**')
    calculate(num1, num2, '%')

//arrowfunctions
//arrays:collection of values stored inside a variabe


